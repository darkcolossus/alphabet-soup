# alphabet-soup
Soup alphabet's like program that finds a term in matrix of f rows and c cols

## Running the program

``` bash
$ go run main.go
```
## Running tests

``` bash
$ cd tests
$ go test -v
```